import { Loading } from "./icons/Loading";

export const LoadingSpinner = () => {
  return (
    <main className="p-5 h-screen flex items-center justify-center text-4xl text-blue-500">
      <Loading />
    </main>
  );
};
